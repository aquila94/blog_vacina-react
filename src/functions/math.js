const header = () => {
    const sum = (a, b) => {
        return a + b;

    };

    return {
        sum,
    };
};

export default math;

//EXPERIENCIA DE TESTE. TESTE NÃO INTEGRADO COM ARQUIVOS. TENTATIVA DE INTEGRAR TESTES NÃO FOI BEM SUCEDIDA. 